<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <!-- View product.template form -->
  <record id="view_discount_product_template_form" model="ir.ui.view">
    <field name="name">Discount Product</field>
    <field name="model">product.template</field>
    <field name="inherit_id" ref="product.product_template_form_view"/>
    <field name="arch" type="xml">
      <xpath expr="//notebook" position="inside">
        <page name="discount" string="Discount">
          <group>

            <group name="customer_discount" string="Sale Discount">
              <field name="customer_discount_product" invisible="not sale_ok" />
            </group>


            <group name="vendor_discount" string="Vendor Disocunt">
              <field name="vendor_discount_product" invisible="not purchase_ok" />
            </group>
            
          </group>
        </page>
      </xpath>
    </field>
  </record>

</odoo>