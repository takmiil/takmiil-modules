<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Wizard Form View -->
    <record id="view_partner_statement_wizard_form" model="ir.ui.view">
        <field name="name">tak.partner.statement.wizard.form</field>
        <field name="model">tak.partner.statement.wizard</field>
        <field name="arch" type="xml">
            <form string="Partner Statement">
                <sheet>
                    <div class="alert alert-primary" role="alert">Select Customer or Vendor</div>
                    <group>
                        <field name="partner_ids" widget="many2many_tags"
                        options="{'no_create': True}"
                        placeholder="Leave empty for all customers and vendors..." />
                    </group>
                    <group>
                        <group>
                            <field name="date_from" />
                            <field name="date_to" />
                        </group>
                        <group>
                            <field name="partner_type" />
                            <field name="include_invoice_details"/>
                            <field name="currency_ids"
                            widget="many2many_tags"
                            options="{'no_create': True}"
                            required="1" />
                        </group>
                    </group>
                </sheet>
                <footer>
                    <button name="action_print_report"
                        string="Print Statement"
                        type="object"
                        class="btn-primary" />
                    <button string="Cancel"
                        class="btn-secondary"
                        special="cancel" />
                </footer>
            </form>
        </field>
    </record>
    <!-- Action -->
    <record id="action_partner_statement_wizard" model="ir.actions.act_window">
        <field name="name">Partner Statement</field>
        <field name="res_model">tak.partner.statement.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
    <!-- Menu Item -->
    <menuitem id="menu_partner_statement_wizard"
        name="Partner Statement"
        parent="account.account_reports_partners_reports_menu"
        action="action_partner_statement_wizard"
        sequence="20" />
</odoo>